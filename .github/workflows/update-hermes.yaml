name: Update hermes version

on:
  schedule:
    - cron: 0 1 * * * # Everyday at 1am
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    name: Update version
    uses: ahobsonsayers/github-workflows/.github/workflows/docker-update-app-version.yaml@main
    with:
      app-name: Hermes
      dockerfile: images/hermes/Dockerfile
      version-arg: HERMES_VERSION
      version-source: github
      version-source-github-repo: NousResearch/hermes-agent
