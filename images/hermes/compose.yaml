services:
  hermes:
    container_name: hermes
    image: arranhs/hermes:latest
    command: ["hermes", "gateway", "start"]
    restart: unless-stopped
    volumes:
      - ~/.hermes:/home/agent/.hermes
      - ~/.gitconfig:/home/agent/.gitconfig
